import React from "react";
import ReactDOM from "react-dom";

import OS from "./OS";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <OS />
  </React.StrictMode>,
  rootElement
);
